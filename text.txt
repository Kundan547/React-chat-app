sudo apt-get update && sudo apt upgrade -y
#after updated the system 


#Install the react
npm install 

#after the installation check the version.
npm -V

#Now install docker and docker-compose file.
sudo apt-get install docker.io docker-compose.

#Now provide the aws creds.
Access key and secret key for aws lambda and api gateway.

